name: selection_model
description: Pure Dart selection state and range selection model
version: 0.0.1
homepage: https://github.com/khoadng/selection_model
repository: https://github.com/khoadng/selection_model
issue_tracker: https://github.com/khoadng/selection_model/issues

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
